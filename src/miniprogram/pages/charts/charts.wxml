<!--index.wxml-->
<view class="tabs-wrap">
  <view class="item {{tabIndex===index?'active':''}}" wx:for="{{tabList}}" wx:key="index"  bind:tap="tabClick" data-index="{{index}}">{{item.code}}天</view>
</view>
<view class="container">
  <ec-canvas id="mychart-dom-bar" canvas-id="mychart-bar" ec="{{ ec }}"></ec-canvas>
</view>


